databases:
  - name: clubpulse-db
    databaseName: clubpulse
    user: clubpulse
    plan: free

services:
  - type: web
    name: clubpulse-api
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: clubpulse-db
          property: connectionString
      - key: NODE_VERSION
        value: "24"
      # Set to the deployed frontend origin once Vercel gives you a URL.
      - key: CORS_ORIGIN
        sync: false
